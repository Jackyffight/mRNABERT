# mRNABERT training dependencies.
#
# transformers/peft/accelerate are pinned to the versions the training + fine-tune
# scripts were validated against. Keep these pins for a faithful reproduction run.
# The torch-free core (mrnabert.sequence_codec, mrnabert.streaming) imports on any
# modern Python, and mrnabert.pretrain guards the one API that later transformers
# renamed (is_torch_tpu_available), so tooling/tests work outside the pin; but the
# fine-tune scripts (regression.py / classification.py) still assume the pinned
# TrainingArguments field names (e.g. evaluation_strategy) — bump deliberately.
datasets
evaluate
einops
numpy
pandas
peft==0.5.0
scikit_learn
scipy
tqdm
transformers==4.32.0
sentencepiece
accelerate==0.24.1
